export const metadata = {
  title: 'Full-Stack System Architecture',
  description:
    'Full-stack MERN platform by Brian van Vlymen — role-based access control, Passport.js authentication, MongoDB data design, and Dockerized microservices.',
}

export default function SystemArchLayout({ children }) {
  return children
}
